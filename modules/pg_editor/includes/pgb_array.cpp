#include "pgb_vector.h"
#include "pgb_array.h"
#include "core/variant/array.h"

//Variant *PGB_Arr::get(const Array *arr, int index) {
//	if (index <= arr->size()) {
//		return arr->get(index);
//	}
//	return nullptr;
//}
